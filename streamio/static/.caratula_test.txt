¡Listo! Ahora los resultados mostrarán la carátula de cada película (si el scraper la obtiene).

Para que los cambios se reflejen en Termux:

1. Copia el archivo actualizado `scrapers/yts.py` a `~/streamio/scrapers/yts.py` en Termux.
2. Copia el archivo actualizado `static/app.js` a `~/streamio/static/app.js` en Termux.

Puedes usar scp o copiar el contenido manualmente.

No se creará ninguna carpeta duplicada. El resto de la estructura no cambia.

¿Quieres que adapte los scrapers de 1337x o TPB para intentar obtener carátulas también (si es posible)?